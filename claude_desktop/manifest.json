{
  "cdx_version": "0.2",
  "id": "com.redhat.console.image-builder",
  "name": "image-builder-mcp",
  "display_name": "Image Builder MCP",
  "version": "---VERSION---",
  "description": "Build Red Hat and CentOS linux machine images.\n_REQUIRES_ `podman` to be installed!",
  "author": {
    "name": "Florian Schüller",
    "email": "florian.schueller@redhat.com"
  },
  "homepage": "https://github.com/osbuild/image-builder-mcp",
  "icon": "icon.png",
  "server": {
    "type": "binary",
    "entry_point": "${user_config.CONTAINER_RUNTIME}",
    "mcp_config": {
      "command": "${user_config.CONTAINER_RUNTIME}",
      "args": [
            "run",
            "--env",
            "IMAGE_BUILDER_CLIENT_ID",
            "--env",
            "IMAGE_BUILDER_CLIENT_SECRET",
            "--interactive",
            "--rm",
            "ghcr.io/osbuild/image-builder-mcp:latest"
      ],
      "env": {
            "IMAGE_BUILDER_CLIENT_ID": "${user_config.IMAGE_BUILDER_CLIENT_ID}",
            "IMAGE_BUILDER_CLIENT_SECRET": "${user_config.IMAGE_BUILDER_CLIENT_SECRET}"
      }
    }
  },
  "tools_generated": true,
  "user_config": {
    "IMAGE_BUILDER_CLIENT_ID": {
      "type": "string",
      "title": "console.redhat.com client ID",
      "description": "client ID of a service account of console.redhat.com",
      "required": true,
      "sensitive": true
    },
    "IMAGE_BUILDER_CLIENT_SECRET": {
      "type": "string",
      "title": "console.redhat.com client secret",
      "description": "client secret of a service account of console.redhat.com",
      "required": true,
      "sensitive": true
    },
    "CONTAINER_RUNTIME": {
      "type": "string",
      "title": "Container runtime (podman, docker, etc.)",
      "description": "Container runtime to use. Set a FULL PATH to the executable if there are problems on startup.",
      "required": true,
      "default": "podman"
    }
  },
  "keywords": [
    "iso",
    "installer",
    "linux",
    "red hat",
    "cloud image"
  ],
  "license": "Apache-2.0"
}
